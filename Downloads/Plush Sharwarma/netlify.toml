# Netlify configuration for Next.js static export
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"

# Fallback for client-side routing (essential for SPA behavior)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
